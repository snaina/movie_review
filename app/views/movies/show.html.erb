

<h1><%#= movie.title %></h1>
<%# @movie.each do |movie| %>
  <div class="row">
    <div class="col-sm-6 col-md-2">
    </div>
    <div class="col-sm-6 col-md-6">
      <div class="col-sm-6 col-md-4" >
        <div class="thumbnail">
          <%= image_tag @movie.image.url(:medium)%>

        </div>
      </div>
      <div class="col-sm-6 col-md-8">
        <div class="thumbnail">
        
          <a pg="" href=""><%= @movie.title %></a>
          
          <div class="caption">
            <h3>
              <span class="releasedate"><%= @movie.release_year %>  |  <%= @movie.actor %>, <%= @movie.actress %></span>
            </h3>
            <div class="mrB10 clearfix">
              <span class="txt_cast">Cast:</span>
              <span class="casting"> <%= @movie.descrption %></span>
            
            </div>
            
     <%= rating_for @movie, "visual_effects", disable_after_rate: true %>
            
 

 <div class="row"> <br>
  <%= form_for(@comment, url: user_movie_comments_path(current_user.id, @movie)) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :movie_id, :value => @comment.movie_id %>
       <div class="input-group">
         <%= f.text_field :commenter, class: "form-control", placeholder: "Type comment..." %>
          <span class="input-group-btn"><%= f.submit "Submit", class: "btn btn-default"%>
          </span>
       </div>
  <% end %>
    
    </div>
    </div>
    </p>



   <%#= rating_for_user @movie, current_user, "rating" %>
   <%#= rating_for @movie, "rating", imdb_avg: false, disable_after_rate: true %>
   <%#= rating_for @movie, "original_score", target: "#hintDiv", targetFormat: "Score: {score}" %>
   <%#= rating_for @movie, "visual_effects", disable_after_rate: true %>
  

<div class="caption">
  <div class="">
    <div class="col-sm-6 col-md-6">
    <div class="">
      <h2 class="page-header">Comments</h2>
        <section class="comment-list">
      <div class="mrB10 clearfix">
      <% @movie.comments.each do |comment| %>
    <p>
    <div class="">
    <article class="row">
            
             <div class="col-md-10 col-sm-10">
              <div class="panel panel-default arrow left">
                <div class="panel-body">
                  <header class="text-left">
                    <div class="comment-user"><i class="fa fa-user"></i><%= current_user.id%></div>
                    
                  </header>
                  <div class="comment-post">
                    <p>
                      <%= comment.commenter %>
                    </p>
                  </div>
                  
                </div>
              </div>
            </div>
          </article>
      
   </p>
   </div>
      <% end %>
    </div>           
    </div>
     
<nav aria-label="...">
  <ul class="pager">
   <li> <%= link_to 'Edit', edit_movie_path(@movie) %></li>
    <li><%= link_to 'Back', movies_path %></li>
    <li><%= link_to "Get PDF", movie_path(format: :pdf) %></li>

</li>
  </ul>
</nav>


